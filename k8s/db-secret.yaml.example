# Example database secret
# Copy this file and replace with actual values
# kubectl create secret generic db-credentials --from-file=connection-string=<(echo "postgresql://user:pass@host:5432/dbname")

apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
type: Opaque
stringData:
  connection-string: "postgresql://ideaboard:CHANGE_ME@db-host:5432/ideaboard"

